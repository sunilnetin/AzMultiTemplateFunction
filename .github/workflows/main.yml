name: CI for Azure Functions (.NET 9)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: 🛎️ Checkout code
      uses: actions/checkout@v3

    - name: 🧰 Setup .NET 9 SDK
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '9.0.x'

    - name: 📦 Restore dependencies
      run: dotnet restore

    - name: 🏗️ Build all projects
      run: dotnet build --configuration Release

    #- name: 🧪 Run unit tests
     # run: dotnet test SharedLib.Tests --no-build --verbosity normal

    #- name: 🐳 Build Docker image (optional)
     # run: |
      #  docker build -t httptriggerfunction ./HttpTriggerFunction
        #docker build -t timertriggerfunction ./TimerTriggerFunction
